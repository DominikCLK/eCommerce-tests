export interface NavbarTabTestCase {
  navbarTab: string;
  url: string;
}

export interface CategoriesTabTestCase {
  categories: string;
  url: string;
}

export interface AccountTabTestCase {
  accountTab: string;
  url: string;
}
